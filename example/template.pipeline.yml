---
resources:
- name: github
  type: git
  source:
    uri: https://github.com/jjghali/sga-backend.git    
    username: jjghali
***REMOVED***

jobs:
- name: template
  plan:
  - get: github
    trigger: true
  - task: listFiles 
    config:
      platform: linux
      image_resource:
        type: registry-image
        source: { repository: ubuntu, tag: "bionic" }
      inputs:
        - name: github        
      run:
        path: /bin/sh
        args:
          - -c
          - |            
            ls -al github